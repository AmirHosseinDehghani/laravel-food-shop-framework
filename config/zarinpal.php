<?php

return [
    /*
    |--------------------------------------------------------------------------
    | زرین‌پال - تنظیمات اصلی
    |--------------------------------------------------------------------------
    */

    'merchant_id' => env('ZARINPAL_MERCHANT_ID', 'xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx'),

    // URL که کاربر بعد از پرداخت به آن برمی‌گردد
    'callback_url' => env('ZARINPAL_CALLBACK_URL', '/payment/callback'),

    // توضیحی که هنگام پرداخت نمایش داده می‌شود
    'description' => 'پرداخت از طریق فروشگاه مواد غذایی',

    // فعال‌سازی حالت سندباکس (تستی)
    'sandbox' => true,

    // اگر از درگاه مستقیم زرین‌گیت استفاده می‌کنی، این گزینه را true کن
    'zaringate' => false,
];
